let miRefran = "mas vale tarde que nunca";

let longitudRefran = (miRefran.length);
let novenaletra = (miRefran.charAt(9));
let ultimaLetra = (miRefran.length-1);
let indicedePalabra = (miRefran.indexOf("que"));


//console.log(miRefran)
console.log(longitudRefran)
console.log(novenaletra)
console.log(ultimaLetra)
console.log(miRefran.replace("que","(palabrarobada)"));
console.log(miRefran.toLocaleUpperCase());